import { createReducer } from '@reduxjs/toolkit';
// import {  } from './operations';
// import {  } from './actions';

const initalState = {};

const something = createReducer(initalState, {
 
});

export default something;
